using UnityEngine;

public class GooBarAnimateIn : MonoBehaviour
{
    [SerializeField] float rightValue;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
